---
import '../styles/global.css';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="pt-BR" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Portfolio Acadêmico e Profissional" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&display=swap" rel="stylesheet">
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-canvas text-ink antialiased min-h-screen flex flex-col">
		<main class="flex-grow">
			<slot />
		</main>
        <footer class="py-6 text-center text-sm text-stone-500 border-t border-stone-200 mt-20">
            <p>&copy; {new Date().getFullYear()} Ian Rodrigues. Todos os direitos reservados.</p>
        </footer>
	</body>
</html>
